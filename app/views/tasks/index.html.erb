<% @tasks.each do |task| %>
<%= task.name %>
[<%= link_to "view", task_path(task), class: "your-class" %>]
[<%= link_to "edit", edit_task_path(task), class: "your-class" %>]
[<%= link_to "delete", task_path(task),  :method => :delete, :confirm => "Are you sure?", :title => "Delete #{task.name}", class: "your-class" %>]<br>
<%end%>
